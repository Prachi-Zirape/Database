1. select saleman.name, custmer.cust_name, customer.city
    from salesman inner join customer
    where customer.city=salesman.city

2. select orders.ord_no, orders.purch_amt, customer.cust_name, 
    customer.city from orders, customer
   on orders.customer_id=customer.customer_id
   where orders.purch_amt between 500 and 1200

3. select c.cust_name, c.city, s.salesman_name as "salesman",
     s.commission from customer as c inner join salesman as s
     on c.salesman_id=s.salesman_id

4. c.cus_name, c.city, s.salesman_name as"salesman", s.commission
    from customer c inner join salesman s
    on c.salesman_id=s.salesman_id
   where s.commission>0.12

5. select c.cust_name, c.city, s.salesman_name as "salesman",
    s.commssion from customer c inner join salesman s
    on c.salesman_id=s.salesman_id
   where c.city <> s.city and s.commssion>0.12

6.
SELECT a.ord_no,a.ord_date,a.purch_amt,
b.cust_name AS "Customer Name", b.grade, 
c.name AS "Salesman", c.commission 
FROM orders a 
INNER JOIN customer b 
ON a.customer_id=b.customer_id 
INNER JOIN salesman c 
ON a.salesman_id=c.salesman_id;

7. select * from orders  inner join customer
     on orders.customer_id=customer.customer_id
    inner join salesman
    on orders.salesman_id=salesman.salesman_id
  
  SELECT * 
FROM orders 
NATURAL JOIN customer  
NATURAL JOIN salesman;

8.  select c.cust_name, c.city, c.grade, s.name,, s.city
     from customer c inner join salesman s
     on c.salesman_id=s.salesman_id
    order by c.customer_id

9. select c.cust_name, c.city, c.grade, s.name, s.city
    from customer c left join salesman s
    On c.saleman_id=s.salesman_id
    where c.grade<300
    order by c.customer_id

10. select c.name, c.city, o.ord_no, o.ord_date, o.purch_amt from customer c left join orders o
On c.customer_id=o.customer_id
order y o.ord_date

11.  select c.cust_name, c.city, o.ord_no, o.ord_date,
     o.purch_amt, s.name, s.commission
    from custormer c left join order o 
   on o.customer_id=c.customer_id 
   left join salesman s 
  on s.salesman_id=o.salesman_id

12. select s.name from salesman s left join
      customer c 
      on s.salesman_id=c.salesman_id
     order by s.name
  
13. select s.name, c.cust_name, c.city, c.grade, o.ord_no
        o.ord_date, o.purch_amt
       from salesman s left outer join customer c
      On s.salesman_id=o.salesman_id
     left outer join order o 
    on c.customer_id=o.customer_id

14. select s.name, c.cust_name, o.ord_no,o.purch_amt,
      c.grade from  customer c right outer join salesman s
     on c.salesman_id=s.salesman_id 
    left outer join orders o 
   on s.salesman_id=o.salesman_id
   where o.purch_amt>2000 and c.grade is not null

15. select c.cust_name, c.city, o.ord_no, o.ord_date,
      o.purch_amt from customer c left outer join 
      orders o 
    On o.customer_id=c.customer_id

16. select c.cust_name, c.city, o.ord_no, o.ord_date,
       o.purch_amt from customer c full outer join
      orders o On s.salesman_id=c.salesman_id
     where c.grade is not null 

17. select * from salesman s cross join customer c
      
18. select * from salesman cross join customer
       where salesman.city is not Null

19. select * from salesman cross join customer
      where salesman.city is not null and 
      customer.grade is not null

20. select * from salesman cross join customer
      where  salesman.city <> customer.city  and
      salesman.city is not null and customer.grade is not null

21. select * from company_mast inner join
      join item_mast on com_id=mast_com

22. select i.pro_name, i.pro_price, c.com_name
        from item_mast i left outer join company_mast c 
       on i.pro_com=c.com_id

23. select b.avg(pro_price), a. com_name from 
      company_mast as a inner_join item_mast as b 
    on a.com_id =b.com_id 
   group by a.com_name
     
24. select b.pro_price, a.com_name from company_mast as a
      inner join item_mast as b on 
      a.com_id=b.pro_com 
      group by com_name
    having b.pro_price>350

25. select a.pro_name, a.pro_price, f.com_name
      from item_mast a inner join company_mast f
      on a.pro_com=f.com_id
      and a.pro_price
     (
       select max(a.pro_price)
       from item_mast a
       where a.pro_com=f.com_id)

26. select * from emp_department inner join emp_details
      on dpt_code=emp_dept

27. select b.emp_fname, b.emp_lname, a.dpt_name, a.dpt_allotment
       from emp_department as a inner join emp_details as b
       on a.dpt_code=b.emp_dept

28. select a.dpt_name, a.dpt_allotment b.emp_fname, b.emp_lname
       from emp_department as a inner join emp_details as b
       where dpt_allotment>5000

29. select a.dpt_name, count(b.emp_idno) from 
       emp_department as a inner join emp_details as b
       group by a.dpt_name
     having count(b.emp_idno)>1
































      























































